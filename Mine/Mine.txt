# 1. Initialize repo and configure remote
git init
git remote add origin https://github.com/petermanoukian/collegelessons.git
git branch -M main

# 2. Stage main branch (ignores Mine/ and zips/ automatically)
git add .
git commit -m "Initial commit with Lessons2 and 3"
git push -u origin main

# 3. Create and push Lesson2 branch from main
git checkout -b Lesson2
git push -u origin Lesson2

# 4. Switch back to main, then create and push Lesson3 branch
git checkout main
git checkout -b Lesson3
git push -u origin Lesson3

git checkout main

git rm --cached .gitignore
git commit -m "Remove .gitignore from repository"
git push origin main

git clone https://github.com/petermanoukian/collegelessons.git
cd collegelessons


git add .
git commit -m "Add Mine folder"
git push origin main


git rm .gitignore
git commit -m "Remove gitignore file"
git push origin main


git add .
git commit -m "Add Lesson 4"
git push origin main


git checkout -b Lesson4
git push -u origin Lesson4

git checkout -b frompc
git add .
git commit -m "upload from PC"
git push -u origin frompc 

git add .
git commit -m "upload from PC - Lesson 5 Add the Database"
git push -u origin frompc 

git add .
git commit -m "upload from PC - Lesson 6 searches"
git push -u origin frompc 

git add .
git commit -m "upload from PC - Lesson 6 searches Plus Mine.txt"
git push -u origin frompc 


git add .
git commit -m "upload from PC - Lesson 7 file and image upload, edit, update delete"
git push -u origin frompc 

git add .
git commit -m "upload from PC - Lesson 7 add the latest database"
git push -u origin frompc 


git add .
git commit -m "Lesson 8"
git push -u origin frompc 